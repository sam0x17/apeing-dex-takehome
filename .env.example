# Polygon RPC for balance/allowance reads (falls back to publicnode)
NEXT_PUBLIC_POLYGON_RPC=

# Li.Fi integrator id (falls back to "sam-johnson-assessment")
NEXT_PUBLIC_LIFI_INTEGRATOR=

# Optional: Arbitrum RPC for source-chain balance reads (falls back to publicnode)
NEXT_PUBLIC_ARBITRUM_RPC=
